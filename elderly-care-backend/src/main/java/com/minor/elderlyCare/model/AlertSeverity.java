package com.minor.elderlyCare.model;

/**
 * Severity levels for health alerts.
 *
 * WARNING  — value slightly outside normal range; informational.
 * CRITICAL — value significantly outside normal range; immediate attention needed.
 */
public enum AlertSeverity {
    WARNING,
    CRITICAL
}
